# Environment
NODE_ENV=test

# App
APP_NAME=fastify-ts-api

# Logging (keep quiet during tests)
LOG_LEVEL=error

# Database (connect from HOST to Docker DB)
DATABASE_URL=postgresql://appuser:apppassword@localhost:5432/appdb
DB_POOL_SIZE=5
